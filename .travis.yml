language: node_js
cache:
  directories:
    - node_modules
branches:
  only:
    - master
jobs:
  include:
    - stage: release
      node_js: lts/*
      script:
        - npm run test
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release
